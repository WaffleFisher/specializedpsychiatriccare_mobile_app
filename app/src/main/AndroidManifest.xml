<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="care.specializedpsychiatriccare">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="care.specializedpsychiatriccare.Tele" />
        <activity android:name="care.specializedpsychiatriccare.Meet" />
        <activity android:name="care.specializedpsychiatriccare.Schedule" />
        <activity android:name="care.specializedpsychiatriccare.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
